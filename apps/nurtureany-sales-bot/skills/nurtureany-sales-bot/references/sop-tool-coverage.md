# SOP Tool Coverage

Use this reference with `sales-best-practices.md` before changing or answering from NurtureAny tools. It maps the live V1 MCP surface to the sales SOP so every tool keeps the same source hierarchy, access, safety, cost, and mutation boundary.

## Global SOP Rules

- Source hierarchy: HubSpot and live tool output override training docs for target-account membership, owner, country, contract end date, current tools, follow-up status, calls, meetings, tasks, notes, conversations, campaigns, and deals. Aircall is selected-call enrichment only and never overrides HubSpot account/activity truth.
- HubSpot override fields: `hs_is_target_account`, `hubspot_owner_id`, `company_country`, `contract_end_date`, `current_tools`, HubSpot communications/notes/tasks/meetings/calls, Conversations threads, Marketing Campaigns, and configured QO/QO Met/closed-won deal stages.
- Gong public coaching docs are design inspiration only for selected-call scorecard structure and manager feedback loops. Do not claim Gong integration, call Gong APIs, add Gong credentials, or treat Gong as a StaffAny data source.
- OpenAI voice docs are technical evidence for possible transcription/realtime architectures only. Do not claim live sales coaching, WhatsApp call listening, or Aircall live-listen unless an approved live audio source, consent policy, and realtime adapter exist.
- ElevenLabs voice docs are alternative/future voice-stack evidence only. Do not claim active ElevenLabs integration, credentials, SIP/Twilio routing, Aircall/WhatsApp audio access, or live coaching unless an approved adapter, consent policy, retention policy, and evals exist.
- Intent-gated Slack flow: first tool-backed Slack request plans and waits for `run` unless quick-autorun is fully satisfied by obvious bounded Slack context, exact scope, read-only or preview/draft-only work, expected runtime under 60 seconds, and a small bounded tool set. A selected current-thread or permalink read may happen before `run` to interpret the request or write the preflight, but business-data, paid, write, and send tools remain gated. Material scope changes need a revised plan.
- Access scope: Slack email is caller identity only; access comes from `NURTUREANY_ACCESS_POLICY_PATH`, HubSpot owners API, and country/owner scope.
- Explicit `partnerships_viewers` entries are country-scoped read-only target-account/account-context viewers only.
- Regional event operators are explicit `event_operators` policy entries. They can use `find_event_sourcing_target_accounts` for read-only in-country AE account sourcing and `get_luma_event_match_keys(include_contact_pii=true)` plus `find_target_accounts_by_luma_match_keys(include_contact_pii=true)` for read-only Luma RSVP account resolution; matched scoped HubSpot contact email/phone/mobile may be returned for exact contact-email matches only. They cannot use manager/admin tools, coaching, revenue metrics, HubSpot writes, task primitives, generic account context, unmatched attendee exports, raw registration answers, raw match-key lists, message bodies, or external sends.
- PII/body safety: never return raw Slack transcripts, raw HubSpot rows, communication bodies, note bodies, task bodies, call/meeting bodies, attendee exports, phone exports, raw images, raw registration answers, raw match-key lists, or secrets by default. Selected manager/admin tools may inspect `hs_communication_body` internally for classification only, such as event follow-up matching or `audit_owner_whatsapp_kns_window`; raw bodies still must not be returned.
- Cost/credit reporting: Tavily public research and Exa must return `cost_report`; Lusha and Prospeo must return `credit_report`. No hidden paid enrichment.
- Mutation policy: V1 exposes read, preview, selected approval-gated enrichment, selected approval-gated Eazybe template-send tools, and narrow approval-gated HubSpot Task primitives only. `create_hubspot_task`, `append_hubspot_note`, and `update_nurture_fields` are generic planned write-phase tools and stay disabled in V1.
- Sales-best-practices usage: apply `sales-best-practices.md` for Friday review, operating rhythm, QO/QO Met quality, inbound/routing, warm activity, events, pre-demo/demo/post-demo, coaching, AI/data readiness, and nurture drafting.
- Reviewed lessons: reusable corrections may be recorded only as `pending_review` runtime candidates. They do not override HubSpot, access policy, Slack identity rules, safety, approval gates, or repo references until promoted into the repo packet, verified, deployed, and live-checked. Honcho remains disabled.
- Inbound/routing: consider lead source, ICP fit, buying role, current tools, clean-lead completeness, and QO/QO Met quality before treating inbound as sales-ready.
- Event attribution: do not attribute QO, QO Met, deals, or follow-up to an event unless HubSpot stages/tags and event-specific evidence verify it; otherwise mark `needs-check`.
- AI/data readiness: clean CRM/activity data comes before automation advice.

## Tool Coverage Matrix

| Tool | Group | SOP coverage |
| --- | --- | --- |
| `read_recent_slack_intent_context` | Slack intent | Read-only configured-channel context for quick-autorun routing only; max 10 messages or 30 minutes, safe summaries/permalinks only, no raw transcript persistence, no user-token or Slack-connector fallback, no Slack mutation. |
| `get_current_slack_thread_context` | Slack thread | Explicit selected-thread read before `run`, after `run`, or during bounded continuation when the current thread is needed to interpret the request or answer; one selected public or configured thread-context channel, max 50 messages, bot token only, no Kai Yi channel membership dependency, may auto-join public source channels via `conversations.join`, safe summaries/permalinks only, no private-channel bypass, raw transcript export, broad history/search, user listing, posting, reactions, pins, Slack-connector fallback, user-token fallback, or mutation. |
| `get_selected_slack_thread_context` | Slack thread | User-supplied permalink read before `run`, after `run`, or during bounded continuation when the selected thread is needed to interpret the request or answer; parse one selected public or configured thread-context channel, max 50 messages, bot token only, no Kai Yi channel membership dependency, may auto-join public source channels via `conversations.join`, safe summaries/permalinks only, no private-channel bypass, raw transcript export, broad history/search, user listing, posting, reactions, pins, Slack-connector fallback, user-token fallback, or mutation. |
| `extract_inbound_lead_alerts` | Slack inbound | Configured public inbound alert channel read only, via `NURTUREANY_INBOUND_ALERT_CHANNEL_IDS`; may auto-join configured public channels, max 50 alert messages, returns alert time, permalink, source, tagged Slack user IDs, redacted summary, safe lead hints, and masked phone last-four only; no raw transcript, raw phone numbers, broad history/search, private-channel bypass, Slack mutation, user-token fallback, or Slack-connector fallback. |
| `audit_standup_down_accountability` | Slack accountability | Plan-first Slack-only audit for one configured public channel/date from `NURTUREANY_STANDUP_AUDIT_CHANNEL_IDS`; builds expected roster from active Slack channel members intersected with prior stand-up/down participants, returns safe per-person status/permalinks only, may join a configured public channel, and never returns raw note bodies, browses arbitrary history, reads private channels, posts Slack messages, mutates HubSpot, uses user tokens, or uses the Slack connector. |
| `list_inbound_threads` | Inbound | HubSpot Conversations summaries only; apply inbound/routing SOP, access scope, plan-first flow, PII/body safety, no mutation. |
| `get_inbound_thread_context` | Inbound | One selected thread only; HubSpot override fields govern account truth; no bulk export, no raw-body dump, no mutation. |
| `resolve_inbound_slack_alerts_to_hubspot` | Inbound | Safe Slack alert rows resolve to HubSpot Conversations/contact/company by safe IDs first, then bounded candidate matching on timestamp plus name/company/domain hints; returns verified/candidate/unmatched, HubSpot account status, optional verified-customer exclusion bucket, no mutation or raw body/phone export. |
| `audit_inbound_sla` | Inbound | Admin/manager inbound SLA audit only; safe aggregate routing/ack/first-touch evidence plus safe per-row lead context, resolver summary, verified customer exclusion, and manual-vs-automated first-touch distinction; no raw phone numbers, bulk raw thread export, or mutation. |
| `list_marketing_campaigns` | Marketing | Campaign metadata is context only; manager/admin access, PII/body safety, no mutation, no revenue attribution without HubSpot proof. |
| `get_campaign_assets` | Marketing | Reads bounded HubSpot campaign assets and safe Forms summaries; raw submissions hidden; campaign assets do not override CRM fields. |
| `get_campaign_social_effectiveness` | Marketing | Reads HubSpot social connected-account status and aggregate `SOCIAL_BROADCAST` clicks for one campaign; social clicks are engagement evidence only, raw channel IDs and bulk post exports stay hidden, and no native social scraping or mutation is allowed. |
| `get_marketing_touch_context` | Marketing | Maps safe marketing touches to scoped HubSpot context; inbound/routing and attribution stay `needs-check` without proof. |
| `get_marketing_campaign_attribution` | Marketing | Searches bounded HubSpot contact source fields and maps only scoped contacts/companies before checking configured QO/QO Met/closed-won deal stages; read `answer.outcome_summary` first so truncation cannot hide visible QO/QO Met/closed-won counts; no raw PII, raw form rows, generic QO-total substitution, or mutation. |
| `audit_hubspot_owner_roster` | HubSpot admin | Uses HubSpot owners API and access policy; admin-only scope; no body/PII exports, no mutation. |
| `list_my_target_accounts` | HubSpot queue | HubSpot target, owner, and country fields; AE-owned scope; completeness metadata; no mutation. |
| `list_team_target_accounts` | HubSpot queue | Manager/admin/partnerships-viewer country scope; optional owner filter preserves caller identity; completeness metadata; no mutation. |
| `find_event_sourcing_target_accounts` | Event sourcing | Regional event operator / manager / admin read-only account sourcing across classified in-country AE target accounts; filters country, industry, headcount range, owner/HR contact-role coverage, deal bucket, and optional owners; no raw emails, phone numbers, HubSpot writes, revenue metrics, coaching, or manager/admin escalation. |
| `resolve_nurture_scope` | Primitive scope | Resolves caller role, canonical email, allowed/requested countries, and optional scoped owner; returns no business metrics. |
| `resolve_sales_owners` | Primitive owners | Resolves active/classified HubSpot sales owners for scoped countries before rep/team metric tools; returns owner IDs/emails/names/countries/timezones. |
| `list_sales_call_events` | Primitive activity | Typed HubSpot calls reader for scoped owners/local windows; returns safe normalized metadata with explicit `association_mode` and `raw_body_returned=false`. |
| `summarize_sales_call_stats` | Primitive reducer | Deterministic call stats for direct count questions; `completed_calls_gt_60s` is strict `>60`, default connected guardrail is completed plus `>=120`, and counts are not based on AE coaching candidate caps. |
| `find_target_accounts_by_luma_match_keys` | Event matching | Luma/Sheet match keys become scoped HubSpot candidate accounts with match order `exact_hubspot_contact_email` -> `exact_email_domain` -> `company_name_candidate`; exact contact/domain matches are verified, company-name candidates need check; event operators may use this read-only for their explicit countries and classified in-country AE owner filters; exact scoped contact-email matches may return matched contact email/phone/mobile. |
| `audit_priority_account_coverage` | Friday rhythm | Uses 120/150, double tap, clean lead, 40 connected calls, warm activity, and QO/QO Met quality; safe activity evidence only. |
| `build_sales_metric_actuals_query` | Revenue actuals | Builds scoped aggregate SQL for QO and revenue actuals; Rev Sheets/Slides are targets/definitions only; execute through `staffany_bigquery.execute_sql_readonly`. |
| `build_hubspot_revenue_funnel_metrics` | Revenue funnel | Read-only HubSpot created-date deal cohort with Sales Outbound/all-outbound, new-business, renewal exclusion, headcount, industry, signed-stage, and manual-correction audit rows; no HubSpot mutation. |
| `build_ae_coaching_audit` | AE coaching | Metadata-only weekly AE audit for 3 QOs set, local-time morning-message coverage, 40 connected calls, and >60s calls without appointment evidence; WhatsApp windows use each rep's access-policy timezone or explicit override and return local/UTC window fields; returns 1:1 preview rows only, no Sheet mutation or call bodies. |
| `audit_owner_whatsapp_kns_window` | Activity quality | Manager/admin exact-owner WhatsApp audit for scoped target accounts in a local time window; uses access-policy timezone or explicit override, internally classifies KNS from HubSpot communication bodies, returns flags only, and never returns raw bodies. |
| `count_owner_whatsapp_sent_today` | Activity count | Direct owner/date count of HubSpot WhatsApp communication metadata for scoped target accounts; use instead of broad Friday/coaching audits for "sent today" counts; no raw bodies, phone numbers, notes, tasks, meetings, or mutation. |
| `build_sales_whatsapp_window_report` | Report generation | Composed SG/MY weekday WhatsApp report with SG-first ordering, owner-local window, target 30 default, access-policy owner/country/timezone truth, no silent SGT fallback, safe rows, and generated `slack_markdown`; ad hoc reruns do not mutate schedule state. |
| `save_sales_whatsapp_window_report_schedule` | Runtime schedule state | Manager/admin profile-runtime schedule writer for the default weekday report; stores report args, `35 10 * * 1-5 Asia/Singapore`, existing production cron, delivery channel, source thread, idempotency key pattern, created/updated actors, and never writes repo state or secrets. |
| `run_sales_whatsapp_window_report_schedule` | Runtime schedule execution | Loads the saved schedule and deterministically calls `build_sales_whatsapp_window_report`; skips weekends for the default report and uses saved idempotent delivery when not dry-run. |
| `post_generated_sales_report` | Constrained Slack delivery | Posts only generated report markdown to allowlisted `NURTUREANY_REPORT_DELIVERY_CHANNEL_IDS` with `SLACK_BOT_TOKEN`, approval marker, idempotency key, operation-ledger before/after checkpoints, duplicate-send blocking, and no user-token/Slack-connector fallback. |
| `prepare_sales_navigator_decision_maker_queue` | Decision-maker handoff | Manual Sales Navigator queue from scoped HubSpot company/contact context; `pre_demo_150` and `post_event_top10` only, no LinkedIn scraping, browser automation, PII reveal, or HubSpot mutation. |
| `build_friday_sales_review` | Friday rhythm | Uses sales best practices for hygiene, funnel, coaching, actions, support, and optional warehouse follow-ups; stage config gates QO/QO Met; no raw bodies. |
| `build_manager_chase_plan` | Manager chase | Manager/admin-only copy-ready chase drafts from HubSpot coverage, task/activity evidence, and optional selected Slack blocker summaries; Manager draft only, no rep tags, raw transcripts, sends, or mutation. |
| `find_aircall_calls` | Call review | Read-only Aircall recent-call lookup for selected call-review workflows; normalizes Aircall `from`/`to` to UNIX timestamps, can do bounded timestamp/user/duration matching when HubSpot lacks `hs_call_external_id`, max 5 output calls, safe metadata and recording availability only, no phone numbers, raw recording URLs, audio, transcripts, mutation, or HubSpot truth override. |
| `resolve_aircall_call_for_coaching` | Call review | Read-only selected-call resolver for natural-language call hints or extracted safe hints. Accepts an exact Aircall ID or bounded timestamp/user/duration hints, returns one safe selected call or candidate choices, and never returns raw recording URLs, phone numbers, audio, transcripts, mutation, or HubSpot truth override. |
| `transcribe_aircall_recording` | Call review | Lower-level selected numeric Aircall call transcription only, preferably from HubSpot `hs_call_external_id`; never use a HubSpot call object ID as the Aircall call ID. Transiently downloads one recording, sends it to OpenAI audio transcription, caps at 25 MB / 60 minutes, deletes audio before return, returns redacted bounded transcript text/segments only, and never mutates Aircall or HubSpot. Use `analyze_aircall_call_coaching` for normal manager-quality call-review answers. |
| `analyze_aircall_call_coaching` | Call review | Composed selected-call coach summary for prompts like `analyze Jeffrey's 2:59 call` after one numeric Aircall call ID is resolved. Transcribes with OpenAI `gpt-4o-transcribe-diarize`, computes talk ratio, longest monologue, turn count, question count, objections, next-step clarity, and customer reaction moments locally from redacted diarized transcript/timing, then uses OpenAI `gpt-5.5` Structured Outputs for the Gong-inspired NurtureAny structure: call summary, 0/1/2 scorecard, timestamped coachable moments, transcript/timing interaction cues, manager coaching note, rep next action, source/scope/confidence/caveat. Returns safe coaching JSON only, says audio-native tone was not checked, never returns raw transcript/audio/recording URLs/phone numbers, never mutates Aircall or HubSpot, never claims Gong integration, and keeps ElevenLabs future/benchmark only. |
| `get_account_context` | Account context | HubSpot account truth plus C360 enrichment for verified customers; safe packet by default; access scope, PII/body safety, no mutation. |
| `build_pre_demo_game_plans` | Pre-demo | Selected scoped accounts only; I-C-BANT and missing-evidence rules; optional Slack source-thread permalink as provenance only; no raw Slack transcript, invented pricing/current tools/case studies, or mutation. |
| `find_sales_case_studies` | Case studies | Read-only approved public customer-story and full-video-reviewed BMC podcast-card lookup for scoped HubSpot accounts or supplied brainstorm queries; no weak analogy promotion, HubSpot mutation, or unapproved name drops. |
| `build_singapore_lead_enrichment_plan` | Lead enrichment | Review-first Singapore lead-enrichment plan over scoped HubSpot target accounts or selected SG HubSpot companies; covers decision maker, champion/influencer, usable-contact, phone verification, field-level mismatch, capped-effective provider-waterfall policy, handoff note, and draft-only WhatsApp readiness; source ladder is HubSpot -> HubSpot notes/tasks/history -> Tavily public company/job-board research -> Exa people candidates -> Lusha domain search -> if 0 results and public LinkedIn URLs are available, Lusha LinkedIn URL lookup -> Prospeo -> approved reveal -> manual Truecaller/call outcome -> HubSpot preview; no paid reveal, Truecaller automation, raw phone export, WhatsApp send, owner reassignment, or HubSpot mutation. |
| `list_active_deals_missing_next_meeting` | Deal hygiene | Direct path for active deals with no next meeting; uses scoped target accounts, associated deals, and future HubSpot meeting associations; no Friday-review/account-coverage detour. |
| `list_sales_followup_tasks` | Follow-up | Existing incomplete sales-owned HubSpot tasks only; safe task fields; no duplicate task creation. |
| `preview_hubspot_sales_task` | HubSpot task preview | Resolves a scoped account/contact/deal, blocks past due dates, checks active duplicates, and returns a safe HubSpot Task create payload preview only; `hs_timestamp` is due truth and raw task body is not returned. |
| `create_approved_hubspot_sales_task` | Approval-gated HubSpot task write | Creates one scoped HubSpot Task only after exact `create task` or `confirm task`; associates company with type ID `192`, optional contact `204`, optional deal `216`; rechecks duplicates before POST. |
| `preview_hubspot_task_update` | HubSpot task preview | Previews only scoped task reschedule/reminder fields (`hs_timestamp`, optional `hs_task_reminders`) or completion (`hs_task_status=COMPLETED`); no mutation. |
| `apply_approved_hubspot_task_update` | Approval-gated HubSpot task write | PATCHes only the approved scoped task fields. Reschedule requires `update task` or `confirm reminder`; completion requires `mark done` or `complete task`; `run`, `ok`, `yes`, `+1`, and `^` are not approvals. |
| `list_due_hubspot_sales_task_reminders` | Reminder read | Reads incomplete scoped sales-owned HubSpot Tasks by `hs_timestamp` for morning/EOD digests; no Sheet, memory, Honcho, Slack reaction, or JSON file becomes task truth. |
| `check_account_followup_status` | Follow-up | HubSpot communications, notes, tasks, and meetings determine status; event attribution requires proof; raw bodies hidden. |
| `check_event_followup_status` | Event follow-up | Luma/Sheet attendance identifies matched accounts; HubSpot owner and customer/prospect/unknown status travel with each account; HubSpot verifies event-specific WhatsApp/tasks; raw attendees and bodies hidden. |
| `record_nurtureany_operation_checkpoint` | Runtime continuity | Profile-runtime ledger checkpoint only; records resumable phase, approval marker presence, idempotency key, and side-effect class without performing any external send or HubSpot mutation. |
| `read_nurtureany_operation_ledger` | Runtime continuity | Reads compact checkpoint state for restart-safe continuation; read-only resume is allowed, but repeated sends/writes stay blocked unless approval marker and idempotency key are both present. |
| `record_nurtureany_lesson_candidate` | Reviewed lessons | Profile-runtime candidate only; writes `pending_review` behavior lessons without changing behavior, using safe summaries only and rejecting raw Slack transcripts, raw HubSpot rows, phone numbers, contact exports, secrets, tokens, and one-off business truth. |
| `list_nurtureany_lesson_candidates` | Reviewed lessons | Reads compact lesson candidates by status; candidates are runtime evidence only and do not override source-packet guidance or HubSpot truth. |
| `read_nurtureany_lesson_candidate` | Reviewed lessons | Reads one safe lesson candidate for review; approved or promoted states still require repo promotion, tests, deploy, and live verification before the bot treats the lesson as behavior. |
| `score_nurture_accounts` | Queue scoring | HubSpot override fields and clean-lead completeness; access scope and pagination caveats; no mutation. |
| `find_contact_gaps` | Enrichment gaps | HubSpot decision-maker and buying-role fields; clean-lead completeness; no paid enrichment or raw PII export. |
| `find_t90_renewal_gaps` | Renewal timing | HubSpot `contract_end_date` is durable timing truth; missing-date bucket is explicit; no raw contacts/task bodies. |
| `generate_free_search_tasks` | Public evidence | Review-only public/manual tasks after HubSpot scope; no paid calls, social scraping, or mutation. |
| `research_public_company_signals` | Public evidence | Tavily Search/Extract public company research only after scoped HubSpot company inputs; return `cost_report`, `manual_check_items`, `missing_evidence`, and `will_mutate_hubspot=false`; no gated/social scraping, private HubSpot notes/tasks/contact PII export, or mutation. |
| `find_brand_parent_candidates` | Public evidence | Identity-resolution fallback for unresolved brand/outlet names after direct scoped HubSpot lookup misses; Tavily Search only, returns parent/group candidates and `suggested_hubspot_queries`, then requires scoped HubSpot re-query before news research; no outreach drafting, scoped-account grant, gated/social scraping, PII export, or mutation. |
| `review_public_enrichment_evidence` | Public evidence | Public snippets support but do not override HubSpot; social/gated evidence remains manual-check; no mutation. |
| `scan_drive_event_photos` | Photo/event | Drive/Slack source pointers plus Luma date context; raw-image ban; person association requires human confirmation. |
| `propose_photo_people_matches` | Photo/event | Scoped HubSpot contact/company matching plus uploader clues; human confirmation required; no mutation. |
| `plan_event_photo_followup` | Preview | Confirmed photo match preview only; no WhatsApp auto-send; planned writes remain disabled. |
| `draft_nurture_message` | Drafting | Uses CCC, 3C, K/N/S, QO quality, warm activity, and inbound/routing; manual review only; no send or mutation. |
| `plan_hubspot_writeback` | Preview | Preview-only generic note/field mutation boundary; requires scoped HubSpot IDs and explicit future approval; managers remain blocked from generic team write-back previews. HubSpot Task writes use the separate task primitives only. |
| `list_google_calendar_events` | Calendar | Calendar is enrichment after HubSpot scope; read-only `team@staffany.com`; bounded metadata only; no event mutation/export. |
| `audit_google_calendar_meeting_quality` | Calendar | Uses HubSpot `calendar_audit_seed` and attendee hashes internally; safe names/roles only; follow-up via HubSpot. |
| `list_luma_events` | Luma | Exact tags first; Luma is event enrichment only; no raw guest lists or event mutation. |
| `get_luma_event_match_keys` | Luma | Event-first key extraction; `include_contact_pii=false` returns domains/company names only, while `include_contact_pii=true` adds bounded attendee emails as transient exact HubSpot contact match keys; no attendee names, phones, registration answers, or raw key list in Slack. |
| `get_luma_event_context` | Luma | Requires scoped HubSpot companies; attendance is `checked_in_at`; matched scoped attendee data only; no raw exports. |
| `search_exa_people_candidates` | Paid enrichment | Scoped HubSpot companies only; public candidates; `cost_report` required; no profile scraping, email/phone reveal, or mutation. |
| `search_lusha_decision_maker_candidates` | Paid enrichment | Scoped HubSpot companies only; `credit_report` required; availability flags only; no PII reveal or mutation. |
| `search_lusha_candidates_by_linkedin_urls` | Paid enrichment | Public LinkedIn profile URL lookup only after scoped HubSpot company IDs are passed and validated against HubSpot; `credit_report` required; candidate IDs and inferred name/title only; no LinkedIn scraping, email/phone reveal, raw contact data, Slack PII summary, or mutation. |
| `reveal_lusha_contact_details` | Approval-gated enrichment | Selected contacts plus approval marker; selected PII only in internal Slack; phone reveal requires explicit flag; no HubSpot mutation. |
| `get_lusha_credit_usage` | Paid enrichment | Credit reporting only; `credit_report` required; no prospect PII or mutation. |
| `search_prospeo_decision_maker_candidates` | Paid enrichment | Scoped HubSpot companies only; `credit_report` required; candidate context only; no email/mobile reveal or mutation. |
| `search_prospeo_candidates_by_linkedin_urls` | Paid enrichment | Public LinkedIn profile URL lookup only after scoped HubSpot company IDs are passed and validated against HubSpot; uses Prospeo matching with email/mobile disabled and sanitized output; `credit_report` required; candidate IDs and inferred name/title only; no LinkedIn scraping, raw contact data, Slack PII summary, or mutation. |
| `reveal_prospeo_contact_details` | Approval-gated enrichment | Selected Prospeo persons plus approval marker; selected PII only in internal Slack; phone reveal requires explicit flag; no HubSpot mutation. |
| `get_prospeo_credit_usage` | Paid enrichment | Credit reporting only; `credit_report` required; no prospect PII or mutation. |
| `read_google_slides_deck` | Drive material | Read-only Google Slides extraction for approved sales material; no Drive mutation and no raw export beyond bounded text. |
| `list_drive_folder_images` | Drive photo | Drive metadata only from `team@staffany.com`; source pointers and uploader names only; no raw image copy or Drive mutation. |
| `read_google_slides_deck` | Drive deck | Selected Google Slides or Drive-hosted `.pptx` text only through `team@staffany.com`; no public link sharing, edits, comments, raw byte retention, or CRM truth override. |
| `extract_drive_image_clues` | Drive photo | Transient OCR/vision clues only; raw bytes discarded; event/person attribution remains `needs-check` until confirmed. |
| `read_nurture_material_registry` | Material registry | One read-only Google Sheet for Materials, Playbooks, Peer Intros, Speaker/Venue Opportunities, Events, and Review Log; active/approved/live rows only; no Drive mutation. |
| `read_indonesia_event_registration_attendance` | Event fallback | Indonesia LL/HHH Sheet fallback only when Luma check-in is empty or not used; `Attend The Event` is manual attendance evidence; safe rows and match keys only. |
| `preview_eazybe_template_messages` | Eazybe preview | Selected message IDs only; validates approved template payload and ordered params; phone numbers redacted; no send. |
| `send_approved_eazybe_messages` | Approval-gated send | Requires `approval_marker`; sends only approved Eazybe `templateName` plus ordered `templateParams`; partial failures surfaced; no free-form drafts. |
| `check_eazybe_send_status` | Eazybe status | Summarizes accepted/queued/sent/delivered/failed/pending states for the run; HubSpot WhatsApp evidence can also satisfy sent definition. |
| `resolve_known_area_for_near_me` | Near-me | Curated known areas outside HubSpot; no person GPS or employee movement; no mutation. |
| `build_near_me_outlet_matches_query` | Near-me | Builds read-only SQL for curated outlet matches; BigQuery mutation is out of scope. |
| `refresh_google_places_for_known_area` | Near-me | Google Places is live candidate discovery only; Google-only rows are not CRM truth; no mutation. |
| `build_near_me_c360_customer_query` | Near-me | Builds read-only C360 current-customer SQL; no employee GPS/clock rows; HubSpot/C360 source hierarchy applies. |
| `prepare_near_me_seed_review_candidates` | Near-me review | Review-only candidate preparation; approval stays in Slack; writer job rejects unsafe/unlinked rows. |
| `merge_near_me_sources` | Near-me | Merges outlet matches, C360 customers, HubSpot prospects, and Google candidates with confidence caveats; no mutation. |

## Companion BigQuery Tools

`staffany_bigquery.list_dataset_ids`, `staffany_bigquery.list_table_ids`, `staffany_bigquery.get_table_info`, and `staffany_bigquery.execute_sql_readonly` are read-only companion tools. Use them only for bounded schema inspection and read-only SQL returned by approved NurtureAny planning tools. Never run DDL, DML, exports, grants, or raw PII dumps.

## Runtime Boundaries

If a broad HubSpot tool returns `partial_due_to_soft_timeout=true`, answer from the partial scope and stop. Do not chain another broad audit to compensate; narrow the next run by owner, country, date, or direct-purpose tool.
