# Tasks: Add PSM Ops Google Geocode Capability

- [x] Add OpenSpec proposal, design, tasks, and spec files.
- [x] Add `psm_google_geocode` MCP server and unit tests.
- [x] Wire PSM Ops manifest, config template, profile inventory, health checks, and verifier.
- [x] Add skill, SOUL, Slack runtime, and runtime contract documentation.
- [ ] Run `openspec validate add-psm-ops-google-geocode --strict` before implementation.
- [ ] Run `openspec validate add-psm-ops-google-geocode --strict` before archive.
- [ ] Run `openspec validate add-psm-ops-google-geocode --strict` once the OpenSpec CLI is available.
- [ ] Deploy to `hermes-psm-ops-bot-poc` after the geocode credential is provisioned on the cloud runtime user or Secret Manager.
